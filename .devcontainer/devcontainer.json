{
  "name": "EV Insight Development",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",   // or match your Python version
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y fonts-symbola && pip install -r requirements.txt"
}
